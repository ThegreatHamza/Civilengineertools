export const conversions = {
  mmToM: (value) => Number(value) / 1000,
  mToMm: (value) => Number(value) * 1000,
  kgToTon: (value) => Number(value) / 1000,
  tonToKg: (value) => Number(value) * 1000
};
